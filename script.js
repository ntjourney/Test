let cat='Döner';const $=id=>document.getElementById(id);function empty(v){return !v||v.includes('[')}function link(el,url,label){if(!el)return;if(empty(url)){el.href='#';el.title=label+' wird noch hinterlegt';el.onclick=e=>{e.preventDefault();alert(label+' wird von AZAD GRILL noch hinterlegt.')}}else el.href=url}function render(){let cats=[...new Set(AZAD.menu.map(x=>x.category))];$('tabs').innerHTML=cats.map(x=>`<button class="${x===cat?'active':''}" onclick="cat='${x}';render()">${x}</button>`).join('');$('items').innerHTML=AZAD.menu.filter(x=>x.category===cat).map(x=>`<div class="card"><b>${x.name}</b><br>${x.description}<i style="float:right">${x.price}</i></div>`).join('');$('contact').innerHTML='Telefon: '+AZAD.contact.phone+'<br>Adresse: '+AZAD.contact.address+'<br>Instagram: '+AZAD.contact.instagram;$('address')&&($('address').textContent='AZAD GRILL · '+AZAD.contact.address);$('hours').innerHTML=AZAD.hours.map(x=>'<p>'+x+'</p>').join('')}render();['o','o2','o3','o4'].forEach(id=>link($(id),AZAD.contact.orderUrl,'Bestell-Link'));link($('phone'),empty(AZAD.contact.phone)?'':'tel:'+AZAD.contact.phone,'Telefonnummer');document.querySelectorAll('section').forEach((x,i)=>{x.style.opacity='0';x.style.transform='translateY(12px)';setTimeout(()=>{x.style.transition='opacity .45s,transform .45s';x.style.opacity='1';x.style.transform='none'},120+i*45)});